# CLAUDE.md

## 项目背景
水下目标检测研究项目（RUOD 数据集，10 类，YOLOv11n 基线）。
当前方案：DCAv2（Beer-Lambert 通道注意力）+ CAG-DCN（对比度门控可变形卷积）+ CAL（对比度感知损失）。
三个创新点分别在特征提取（backbone）、特征融合（neck）、优化目标（loss）三个层面。
模块代码保留在 nn/modules/ 下，仅 DCAConv/DCAConv_v2/CAGDCN 注册。
笔记本开发 + 服务器跑实验，代码手动拷贝同步。

## 语言
- 所有回复请使用**简体中文**。

## 思维方式
1. **第一性原理思考**：不接受任务表面的样子，先回到它要解决的根本问题。
- **挖根本问题**：先问"这到底在解决什么"，而非"怎么做这个功能"。
- **质疑前提**：前提存疑就先纠正前提，再执行；别在错的地基上往下做。
- **拒绝过度设计**：每个动作都要问"是否真的服务根本目标"，警惕为做而做、与现有能力重叠的方案。
- **收口到最小必要**：方案膨胀时主动收回到这一轮真正必要的范围；有更短路径主动说明，但未经确认不执行。
2. **不确定就问，别猜。**
3. **反复受阻就升维。** 同一个问题连续三次仍未解决，停止在原思路里重试——跳出执行和检查的细节，从更系统、更全局的层面重新审视：是不是前提错了？是不是问题定义错了？是不是该换条路径？

## 执行纪律
4. **最小化改动**：没要求的不写，只改被要求的部分。如果认为其他地方也需要改，说出来，等确认。
5. **探索前先报告**：调查或阅读代码库时，先列出打算读的文件范围，等确认后再行动。
6. **简洁优先**：能删代码就不加代码。找根因，不打补丁。
7. **轻量化第一**：新模块设计第一步——估算参数量 vs 被替换模块。不允许 >1.5× 被替换模块的参数量。如果超了，先想能不能拆掉冗余结构（MLP→learned scalar、多分支→单分支、per-channel→per-group）。先交轻量版再考虑加回去。

## 完成后输出
7. 改了什么
8. 没改什么（如有必要说明原因）
9. 如何验证结果（命令 / 文件位置 / 需手动检查的点）
10. **模块提交后必须二次审视**：写完一个模块代码后，重新通读，检查：门控是否会退化崩溃、物理可解释性能否站住、各分支初始化时是否等价恒等、梯度路径是否独立不冲突、边界条件（s→0 / s→1）行为是否合理。列出发现的隐患及修复方案。

## 持续改进
10. 如果本次任务中我纠正了你的行为，主动建议一条可以写入此文件的规则，防止重复犯同样的错。

## 任务模式约定（可选前缀）
- 无前缀 → 正常执行
- "方案：" → 只讨论，不写代码
- "快速：" → 跳过解释，直接给结果

## Effort 级别
- 此项目默认使用最大 effort 级别（复杂科研任务需要最深度推理）。
