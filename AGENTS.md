# AGENTS.md

## 项目说明

这是一个 YOLO / Python 项目。Codex 在修改代码前，需要先阅读项目结构，确认入口文件、配置文件、训练脚本、推理脚本和依赖文件。

## 工作规则

- 默认使用中文回答。
- 探索项目前，先简要说明准备查看哪些内容以及目的。
- 回答优先简洁、直接，先给结论，再补充必要细节。
- 修改文件前，先说明准备修改哪些文件和原因。
- 创建和删除新的模块文件前，必须先询问用户并获得确认；未经确认时优先修改已有模块或提出方案。
- 尽量最小化改动，优先做与当前任务直接相关的局部修改，避免无关重构。
- 优先使用项目已有的代码风格、配置方式和命令入口。
- 方案设计优先轻量化，避免为了小问题引入复杂抽象、新框架或重依赖。
- 不要随意删除数据集、权重文件、训练结果，以及 `runs/`、`datasets/`、`weights/` 等目录。
- 不要随意更改数据集路径，除非用户明确要求。
- 涉及训练、推理、评估时，先确认使用哪个配置文件。
- 涉及模型权重、数据集 YAML、类别名称、图片尺寸、batch size、epoch 等训练参数时，修改前先说明影响范围。
- 修改 Python 代码后，优先运行最小验证命令，不要直接启动长时间训练。
- 如果需要安装新依赖，先说明原因，并等待用户确认。
- 如果发现报错，先定位原因，再给出修复方案。

## 模型使用策略

- 平时使用默认普通模型，避免不必要的额度消耗。
- 遇到复杂模块设计、模块二次审视、论文创新点论证、实验方案取舍等高推理任务时，建议切换到 `gpt-5.5` 并设置 `model_reasoning_effort = "xhigh"`。
- 切换高推理模型前，先说明原因和预期收益；如果当前任务不需要高推理模型，优先继续使用普通模型。

## 完成后输出

完成任务后，输出内容应包括：

1. 改了什么。
2. 没改什么；如有必要，说明原因。
3. 如何验证结果，包括命令、文件位置或需要手动检查的点。
4. 如果涉及模块代码，提交后必须二次审视，并列出发现的隐患及修复方案。

## 模块二次审视

写完一个模块代码后，需要重新通读并检查：

- 门控是否会退化或崩溃。
- 物理可解释性是否站得住。
- 各分支初始化时是否尽量等价于恒等映射。
- 梯度路径是否独立、清晰，避免互相冲突。
- 边界条件行为是否合理，尤其是 `s -> 0` 和 `s -> 1`。
- 如发现隐患，先给出定位和影响范围，再给出修复方案。
